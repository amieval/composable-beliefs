# plan-4 - docs and skills for the four-type schema

Depends on: plan-3. Mark's README directive (2026-06-10) governs this plan.

## README (this repo)

- Incorporate why-inference.md semi-verbatim: WHY inference is the word,
  positive case only, no contest-with-implication framing. The
  deductive/ampliative boundary, the compound as the one stored entailment,
  the falsification lifecycle as the proof the type exceeds its deps, the
  direction-of-fit test, the glossary line.
- "How the edges define the territory": present types by what their dep edges
  license and what failure means for each (falsified / violated / withdrawn),
  not by content vibes.
- Extend the explanatory register through the README where opportunities exist
  (Mark: "as much of that as we could get in the readme the better") - concrete
  worked moves (the case-7 generalization), payoff-first explanations, the
  relation-not-mental-act framing.
- House rules: no emdashes; no "X doesn't Y, it Z" sentence shape; reader-first
  ordering per the 2026-06-10 README rewrite.

## Other docs

- quickstart.md (../belief-collections): four types, direction-of-fit test,
  lib: as the v2 teaching example.
- CLAUDE.md: regenerated in plan-2; confirm wording reads four types.

## Skills

- /assert: four types; direction-of-fit trichotomy in the authoring guidance;
  known traps (evaluations name the standard or move up; conditional
  prescriptions decompose; deontic vs alethic must).
- /materialize: directives only.
- /assertions: four types in query/traversal reference.
- /present-codepath: contract stops are contract-grade directives (wording).

## Acceptance

`mix cb.generate.claude_md --check` green; README and quickstart consistent
with the live graph wording; skills reference no v1 type names.
